import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#D64E95',
      '100': '#FA5AAD',
      '200': '#ED55A4',
      '400': '#AD3E78',
      '500': '#6E274C',
    },
  },
});

export default theme;
