import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#FA5AAD',
      main: '#D64E95',
      dark: '#6E274C',
      contrastText: '#000',
    },
  },
});

export default theme;